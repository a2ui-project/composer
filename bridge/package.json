{
  "name": "a2ui-bridge",
  "version": "0.1.0",
  "private": true,
  "main": "dist/preview-bridge.js",
  "types": "dist/preview-bridge.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.bridge.json",
    "test": "NODE_ENV=test vitest run",
    "prettier": "prettier --write . | grep -v '(unchanged)'"
  },
  "devDependencies": {
    "jsdom": "29.1.1",
    "prettier": "3.8.3",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
